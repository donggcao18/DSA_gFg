#include <stdio.h>
void swap(int* a, int* b)
{
    int tmp;
    tmp = *a;
    *a = *b;
    *b = tmp;
}

void min_index (int arr[], int start, int end)
{
    return 
}

void selection (int arr[], int n, int index = 0)
{


}

int main ()
{
    int arr[] = {2,1,6,5,24,21};
    int n = sizeof (arr)/ sizeof (arr[o]);

    selection (arr);

    return 0;
}